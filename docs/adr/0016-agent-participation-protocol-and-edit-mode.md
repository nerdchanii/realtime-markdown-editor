---
id: ADR-0016
title: "ADR-0016: 에이전트는 문서 연산 API 와 MCP 로 참여하고, 편집은 권한 상한 안에서 고른 모드 단계로 반영된다"
status: accepted
date: 2026-09-26
gate: G2
decided_by: user
ratified_by: user
ratified_at: 2026-09-26
ratify_by: null
reversibility: one-way
revisit_if: >-
  외부 에이전트 생태계의 표준 프로토콜이 크게 바뀌거나, 제안 검토가 실제 사용에서 병목이 되거나,
  직접 편집으로 사람의 작업이 덮이는 사고가 반복되면 다시 본다.
related_documents:
  - docs/direction/2026-09-24-product-direction-interview.md
  - docs/adr/0010-decision-gates-and-provenance.md
  - docs/adr/0011-data-authority-by-scope.md
  - docs/adr/0012-authorization-policy-and-principals.md
  - docs/adr/0013-document-type-model.md
  - docs/domain/models/principal.md
  - docs/domain/rules/collaboration-boundaries.md
supersedes: []
superseded_by: null
---

# ADR-0016: 에이전트는 문서 연산 API 와 MCP 로 참여하고, 편집은 권한 상한 안에서 고른 모드 단계로 반영된다

> **accepted (2026-09-26)**
>
> - 비교안과 추천안은 에이전트가 작성했다.
> - [user] 2026-09-26: 편집 기본 동작을 모드 단계로 둔다(후보안 3).
> - [user] 2026-09-26: 나머지 질문(Q1–Q5, Q7–Q13)은 모두 추천안대로 확정했다. 추천이 명시되지 않았던 항목은 초안에 가장 가까운 안을 택했고, "결정된 질문" 절에 표시했다.
> - 아래 "후보안" 절의 `[agent]` 추천은 결정 과정의 기록이다. 확정 내용은 "결정 요약"과 "결정된 질문" 절이 기준이다.
> - 추적 Issue: #4

## 맥락

### 사용자 의도

- [user] 제품은 사람과 AI 에이전트가 같은 대상을 동시에 편집하는 ADE(Agentic Dev Environment) 표면이다(인터뷰 §1).
- [user] 에이전트의 신원은 사용자의 대리인(delegated)일 수도, 독립 참여자(member)일 수도 있다(인터뷰 §4).
- [user] 에이전트는 로컬과 서버 어디서든 실행될 수 있다. **중요한 것은 프로토콜이다**(인터뷰 §4).
- 에이전트가 사용자 문서를 고칠 때의 기본 동작은 인터뷰 기록에 "질문의 대상이 모호했다"고 남아 있었다.
  그래서 이 ADR 은 "제안이냐 직접이냐"를 하나의 질문으로 묻지 않고, **누가, 어떤 상황에서** 편집하는지로 나눠서 물었다.
- [user] 2026-09-26: 기본 동작을 하나로 고정하지 않는다. 권한 모델에 따라 달라지는 모드 단계(수동 / 편집 수락 / 자동 / 자동+크루즈 / 모두 허용)로 둔다(후보안 3).

### 이미 확정된 제약

- **principal 모델** — ADR-0012 §3, [user]
  - `Principal = User | LocalUser | Agent`. 에이전트는 delegated 와 member 두 유형이다.
  - delegated agent 의 실효 권한은 (위임한 사용자의 권한) ∩ (사용자가 준 scope) 다.
  - 모든 쓰기에는 actor `{ principal, onBehalfOf? }` 가 남는다. presence, history, audit 에서 사람과 에이전트를 구분한다.
  - 에이전트의 쓰기와 권한 변경은 append-only audit log 에 남긴다(§5).
  - 에이전트의 편집 기본 동작은 이 ADR 에 맡겼다.
- **단일 policy** — ADR-0012 §1, **G1 (추인 대기, `ratify_by: 2026-10-03`)**
  - 권한 판정은 `authorize(actor, action, resource)` 하나로 한다.
  - HTTP, collab WebSocket, internal API, 앞으로의 에이전트 프로토콜이 모두 이 policy 를 부른다.
- **member agent 의 역할 저장** — decision log, **G1 (추인 대기)**
  - member agent 의 역할은 `WorkspaceMembership` 에 저장한다. 다시 볼 조건이 "이 ADR 에서 다른 모델이 필요해지면"이다.
  - 이 ADR 의 추천안은 이 모델을 **그대로 유지**한다.
- **쓰기 경로** — ADR-0011 결정 2, [user]
  - 문서 편집 상태의 write 정본은 Yjs document 다.
  - direct REST write 로 projection 을 고치지 않는다. 서버 쪽 Yjs mutation 을 거친다.
- **local 범위의 서버 접근** — ADR-0011, [user]
  - local 범위의 opt-in sync 에서 서버는 내용을 읽을 수 있다. 그래서 서버 에이전트를 쓸 수 있다.
  - 계정 없이 local 범위를 쓸 수 있다.
- **본문 정본** — ADR-0013 §2, [user]
  - `markdown` 타입의 본문 정본은 `Y.Text`(Markdown 문자열)다.
  - 선택 이유 가운데 하나가 "에이전트의 텍스트 도구(grep, diff, 텍스트 편집)와 가장 자연스럽게 맞는다"였다.
  - core 는 문서 타입을 모른다. 본문 구조를 아는 일은 type module 이 맡는다.
- **기존 경계 규칙** (사용자 결정은 아니다)
  - `docs/domain/rules/collaboration-boundaries.md` 는 MCP server adapter 를 앞으로의 에이전트 통합 표면으로 두고,
    "MCP tools/resources 는 application use case 를 호출한다"고 적는다.

### 현재 구현

- 에이전트, API token, service account 같은 사람이 아닌 actor 가 없다.
- 새 앱(`apps/editor`)은 슬라이스 1 이다. 브라우저 저장만 있고 서버 sync 와 협업 인증은 슬라이스 2 에서 붙인다.
- 제안(suggestion), 댓글, 변경 검토 기능이 없다. 과거 backlog 에서도 보류되었다(아래 "참고 자료").
- Yjs update 에는 작성자 정보가 없다. 누가 보냈는지는 연결의 clientID 로만 알 수 있다.

### 이 ADR 이 정해야 하는 것

사용자가 에이전트와 함께 일할 때 겪는 흐름을 따라가면 다섯 가지를 정해야 한다.

1. **연결**: 에이전트가 문서를 읽고 쓰려면 어떤 계약으로 붙는가.
2. **인증과 권한**: 에이전트가 누구로서, 어떤 범위까지 할 수 있는가.
3. **편집 반영**: 에이전트의 편집이 문서에 곧바로 들어가는가, 사람이 먼저 보는가.
4. **표시와 되돌리기**: 함께 편집하는 사람이 에이전트의 참여와 변경을 어떻게 알아보고 되돌리는가.
5. **이력과 출처**: 나중에 "이 문장은 누가, 누구의 요청으로 썼나"를 어떻게 답하는가.

## 후보안 1: 연결 프로토콜

### P1. Yjs/Hocuspocus 에 직접 참여

에이전트가 사람의 에디터와 똑같이 협업 서버에 WebSocket 으로 붙는다. Yjs 문서를 받아 로컬에서 고치고 update 를 보낸다.

- **장점**
  - 사람과 같은 경로다. 커서와 presence 가 자연스럽게 생긴다.
  - 문자 단위로 실시간 반영된다. 에이전트가 타이핑하듯 보일 수 있다.
  - 에이전트가 오프라인으로 고친 뒤 나중에 병합할 수 있다.
- **단점**
  - Yjs 바이너리 프로토콜과 문서 레이아웃(`meta`, `content`)이 **외부 공개 계약**이 된다. 협업 엔진과 레이아웃을 바꾸기 어려워진다.
  - 에이전트가 Yjs 라이브러리와 CRDT 개념을 알아야 한다. 대부분의 에이전트는 텍스트 도구로 일한다.
  - update 는 "텍스트의 어느 부분을 바꿨다"는 의도가 아니라 CRDT 연산이다. 서버가 update 단위로 권한과 모드(제안인지 직접인지)를 판정하기 어렵다.
  - 제안 모드를 만들기 어렵다. 보내는 순간 문서에 들어간다.
- **리스크**: 권한 없는 쓰기나 문서 구조를 깨는 update 를 서버가 걸러내기 어렵다. 공개 계약이라 되돌리기도 어렵다.

### P2. 제품이 정의한 문서 API (서버가 Yjs 에 반영)

에이전트는 "문서 읽기", "텍스트 범위 바꾸기", "제안 만들기" 같은 문서 단위 연산을 부른다. 서버가 policy 를 판정한 뒤 Yjs 문서에 mutation 으로 반영한다.

- **장점**
  - 공개 계약이 제품 언어(문서, 텍스트 범위, 제안)다. 협업 엔진이 계약 밖에 있다.
  - 텍스트 연산이라 에이전트의 텍스트 도구와 맞는다(ADR-0013 §2 의 선택 이유와 같은 방향).
  - 모든 호출이 `authorize` 한 곳을 지난다. 편집 모드 판정, audit, 속도 제한도 한곳에서 한다.
  - ADR-0011 의 "서버 쪽 Yjs mutation 을 거친다"는 규칙과 그대로 맞는다.
- **단점**
  - 에이전트 편집이 사람 편집보다 거칠다. 연산 단위로 반영된다.
  - 에이전트의 presence 를 서버가 대신 만들어야 한다(아래 "표시" 참고).
  - 오래 걸리는 편집 중에 문서가 바뀌면 기준 버전을 맞춰야 한다. 텍스트 앵커나 기준 버전 검사가 필요하다.
  - 서버가 없는 local 범위 문서에는 이 API 가 없다.
- **리스크**: API 설계가 부실하면 에이전트가 문서 전체를 통째로 덮어쓰는 식으로 쓰게 된다. 범위 편집을 기본으로 설계해야 한다.

### P3. MCP (Model Context Protocol) 서버

에이전트 생태계에서 널리 쓰는 MCP 로 문서를 resource 와 tool 로 노출한다. 도구를 구현하는 곳은 P2 와 같은 application use case 다.

- **장점**
  - Claude Code 같은 기존 에이전트가 별도 SDK 없이 바로 붙는다. 로컬 에이전트와 서버 에이전트 모두 쓸 수 있다.
  - 기존 경계 규칙(MCP adapter 는 use case 를 호출한다)과 맞는다.
  - MCP 권한 부여 흐름이 OAuth 기반이라 delegated agent 의 "사용자가 scope 를 준다"는 모델과 잘 맞는다.
- **단점**
  - MCP 는 요청-응답 중심이다. 문서 변경을 실시간으로 따라가는 데는 약하다.
  - 명세가 아직 빠르게 바뀐다. MCP 만 공개 계약으로 두면 명세 변화에 제품이 끌려간다.
- **리스크**: MCP 만 있고 그 아래 계약이 없으면, 다른 연결 방식(웹훅, 서버 내부 에이전트)을 붙일 때 로직이 중복된다.

### 추천: P2 를 계약으로, P3 를 첫 번째 연결 방식으로 — [agent]

- **공개 계약은 P2 의 문서 연산(application use case)** 이다. 협업 엔진은 계약에 드러내지 않는다.
- **첫 번째 연결 방식은 MCP** 다. MCP tool 과 resource 는 P2 의 use case 를 부르는 얇은 adapter 다.
  나중에 HTTP API 나 다른 방식이 필요하면 같은 use case 위에 추가한다.
- **P1(Yjs 직접 참여)은 외부 공개 계약으로 두지 않는다.** 제품이 직접 운영하는 신뢰된 에이전트에 한해 나중에 다시 볼 수 있다(Q2, 결정됨).
- 근거
  - 사용자가 말한 "중요한 것은 프로토콜"을, 에이전트가 어디서 실행되든 같은 계약으로 붙는다는 뜻으로 읽었다.
    P2+P3 는 실행 위치와 무관하다. P1 은 협업 엔진을 공개 계약으로 굳힌다.
  - 편집 모드(제안이나 직접)를 서버가 판정하려면 연산의 의도가 보여야 한다. P2 만 그렇다.

## 후보안 2: 인증과 권한

ADR-0012 가 principal 과 역할을 정했다. 이 ADR 은 **에이전트가 연결할 때 어떤 자격증명을 들고 오고, 어떤 action 이 필요한지**만 정한다.

### delegated agent 의 자격증명

- **A1. 동의 흐름으로 받은 scope token (추천)**
  - 사용자가 에이전트를 연결할 때 동의 화면에서 범위(workspace, 문서)와 action(읽기, 제안, 편집)을 고른다.
  - 에이전트는 짧은 수명의 access token 과 회수할 수 있는 refresh token 을 받는다. MCP 의 OAuth 흐름과 같다.
  - 장점: 사용자가 준 scope 가 token 에 명시된다. 교집합 규칙(ADR-0012 §3)을 그대로 계산할 수 있다. 연결 단위로 회수할 수 있다.
  - 단점: 동의 화면과 연결 관리 화면이 필요하다.
- **A2. 사용자가 직접 만든 개인 access token**
  - 설정 화면에서 token 을 만들어 에이전트 설정에 붙여 넣는다.
  - 장점: 구현이 단순하다. CLI 에이전트에 붙이기 쉽다.
  - 단점: token 이 오래 살고 유출되기 쉽다. scope 를 잘 좁히지 않는다.

### member agent 의 자격증명

- owner 나 admin 이 workspace 에 에이전트를 등록하면(`agent.manage`) 그 에이전트의 서비스 자격증명을 발급한다.
- 역할은 `WorkspaceMembership` 에 둔다(decision log G1 을 유지).

### action

- 에이전트에게도 ADR-0012 의 action(`content.read`, `content.write` 등)을 그대로 쓴다.
- **제안을 별도 action 으로 둘지** 정해야 했다(Q5, 결정됨: 새로 둔다).
  - 추천: `content.suggest` 를 새로 둔다. 제안은 "문서를 바꾸지 않고 변경안을 붙이는 쓰기"다.
  - 그러면 "이 에이전트는 제안만 할 수 있다"를 권한으로 표현할 수 있다. 이것이 모드 단계(후보안 3)의 상한이 된다.
- 본문 편집과 구조 변경(생성, 이동, 이름 변경, 삭제)도 에이전트 상한에서 따로 줄 수 있어야 한다(후보안 3).

### 공통

- 모든 에이전트 요청은 `authorize(actor, action, resource)` 를 지난다(ADR-0012 §1, G1 추인 대기).
- 권한이 회수되거나 사용자의 권한이 줄면 에이전트의 다음 요청부터 거부한다. 진행 중인 연결도 끊는다.
- local 범위 문서에서는 기기 소유자(`LocalUser`)가 위임자가 된다. 서버가 없는 local 문서에는 opt-in sync 를 켠 뒤 서버를 통해 붙는다(Q3, 결정됨).

## 후보안 3: 편집 반영 방식

> **이 절은 결정되었다 — [user] 2026-09-26**
>
> - 에이전트 편집의 기본 동작은 하나로 고정하지 않는다. **권한 모델에 따라 달라지는 모드 단계**로 둔다.
> - 모드 단계는 **수동 / 편집 수락 / 자동 / 자동+크루즈 / 모두 허용** 다섯 가지다.
> - "크루즈"는 요청자가 자리를 비워도 긴 작업을 이어가는 모드다. 자동차의 크루즈 컨트롤과 같은 개념이다.
>
> 아래 "권한과 모드의 두 축"과 모드별 동작은 에이전트가 제안했고, 사용자가 방향에 동의한 뒤 추천안 전체를 확정했다(2026-09-26).
> 관련 세부는 "결정된 질문"의 Q7, Q8, Q11, Q12, Q13 이다.

### 처음 비교한 세 방식

처음 초안은 편집 반영 방식을 세 가지로 비교했다. 사용자 결정으로 셋 중 하나를 고르지 않고 모드 단계로 흡수한다.

| 방식 | 흐름 | 함께 편집하는 사람에게 보이는 것 | 모드 단계에서의 위치 |
| --- | --- | --- | --- |
| **E1. 먼저 제안** | 에이전트가 변경안을 만든다. 사람이 수락하면 문서에 들어간다. | 수락 전까지 본문은 그대로다. 변경안이 표시된다. | **수동**. 다른 모드에서 "확인이 필요한 변경"도 이 방식으로 남는다. |
| **E2. 직접 편집, 이력으로 추적** | 에이전트가 문서를 바로 고친다. 변경은 이력에 남고 되돌릴 수 있다. | 바뀐 본문이 바로 보인다. | **편집 수락**의 본문 편집, **자동**의 안전한 작업, **모두 허용** |
| **E3. 적용 후 검토** | 에이전트가 바로 고치되 "검토 대기"로 표시한다. 사람이 거절하면 되돌린다. | 바뀐 본문과 검토 대기 표시가 함께 보인다. | 별도 모드로 두지 않는다(아래 설명). |

- **E1** 은 공유 문서에서 가장 안전하지만, 사람이 모든 변경을 검토해야 해서 긴 작업의 흐름이 끊긴다. 제안 표시, 수락, 거절 UI 가 필요해 구현 비용이 가장 크다.
- **E2** 는 가장 빠르고 구현 비용이 가장 작다. 대신 사람이 쓰는 중인 곳을 에이전트가 바꿀 수 있고, 잘못된 대량 편집을 모든 협업자가 곧바로 본다.
- **E3** 을 별도 모드로 두지 않는 이유는 **협업자 노출**이다. 요청자만 검토하면 되는 한 사람 에디터와 달리, 동시편집에서는 같은 문서를 보는 다른 협업자도 검토 전 변경을 본다.
  이 문제는 E3 만의 것이 아니라 바로 반영하는 모든 모드(편집 수락, 자동, 모두 허용)에 있다.
  그래서 모드 하나를 빼는 대신 **공유 문서에서 허용할 최대 모드를 정하는 방식**(Q7)으로 다룬다.

### 권한과 모드의 두 축 — [agent], 사용자가 방향에 동의

- **권한 (상한)**
  - workspace 나 문서의 owner, admin 이 정한다. delegated agent 는 여기에 위임한 사용자의 권한과 준 scope 의 교집합이 더해진다(ADR-0012 §3).
  - 에이전트에게 의미 있는 action 은 세 묶음이다.
    - `content.suggest`: 제안만 한다(후보안 2, Q5).
    - `content.write` 의 본문 편집: 본문을 바로 고친다.
    - 구조 변경: 문서 생성, 이동, 이름 변경, 삭제. ADR-0012 에서는 `content.write`(생성, 이동, title)와 `content.delete`(삭제)에 걸쳐 있다.
      에이전트 상한에서는 본문 편집과 구분해서 줄 수 있어야 한다. action 을 새로 나눌지 scope 로 나눌지는 구현 설계에서 정한다.
- **모드 (선택)**
  - 요청자가 권한 상한 안에서 **확인 강도**를 고른다.
- **실제 동작 = min(권한, 모드)**
  - 모드를 "모두 허용"으로 골라도 `content.suggest` 만 있는 에이전트는 제안만 한다.
  - 권한이 넓어도 요청자가 "수동"을 고르면 모두 제안으로 남는다.

### 모드별 동작 — [agent] 초안

| 모드 | 본문 편집 | 구조 변경 | 위험한 작업 | 요청자가 없을 때 |
| --- | --- | --- | --- | --- |
| **수동** | 제안 | 제안 | 제안 | 멈춘다(모두 제안으로 남아 있다) |
| **편집 수락** | 바로 반영 | 확인 | 본문이면 바로, 구조면 확인 | 확인이 필요한 곳에서 멈춘다 |
| **자동** | 안전하면 바로 | 확인 | 확인 | 확인이 필요한 곳에서 멈춘다 |
| **자동+크루즈** | 자동과 같다 | 제안으로 쌓고 계속 | 제안으로 쌓고 계속 | 멈추지 않는다 |
| **모두 허용** | 바로 반영 | 바로 반영 | 바로 반영 | 멈추지 않는다 |

- "확인"은 요청자에게 묻고 답을 기다리는 것이다. "제안"은 문서에 변경 묶음(후보안 5)으로 남기는 것이다.
- **위험한 작업**의 초안: 대량 삭제, 다른 사람이 편집 중인 구간, 구조 변경. 판정을 어디서 할지는 Q13 이다.
- **자동+크루즈**
  - 자동과 같은 판정을 쓴다. 다만 확인이 필요한 항목에서 멈추지 않고, 제안(변경 묶음)으로 쌓은 뒤 다음 작업으로 넘어간다.
  - 종료 조건(시간, 변경량, 범위)과 사람이 언제든 멈출 수단이 있어야 한다.
  - 사람이 같은 구간을 편집하기 시작하면 그 구간은 양보한다. 세부는 Q12 다.
- **모두 허용**은 권한 상한 안에서 확인 없이 모두 한다. 되돌리기(후보안 4)와 이력(후보안 5)만이 안전장치다.

### 누가, 어떤 상황에서 편집하는가

인터뷰에서 질문의 대상이 모호했던 이유는 상황마다 알맞은 답이 달라서다. 모드 단계는 이 차이를 요청자가 고르게 한다.

| 상황 | 예 | 위험 | 어울리는 모드 (예시) |
| --- | --- | --- | --- |
| **S1. 사람이 방금 시킨 delegated agent, 요청자가 문서를 보고 있다** | "이 절을 요약해 줘" | 낮다. 요청자가 결과를 바로 본다. | 편집 수락, 자동 |
| **S2. 사람이 시킨 delegated agent, 요청자가 자리에 없다** | 긴 작업을 맡기고 떠난다 | 중간. 다른 협업자가 먼저 본다. | 자동+크루즈 |
| **S3. 스스로 도는 member agent** | 링크 정리, 정기 갱신 | 높다. 누구의 요청도 아니다. | 수동, 또는 admin 이 정한 상한 |
| **S4. local 범위의 개인 문서** | 혼자 쓰는 메모에 에이전트를 붙인다 | 낮다. 다른 협업자가 없다. | 모두 허용까지 |

- 워크플로우가 에이전트를 실행하는 것도 시작 방식의 하나다. 워크플로우의 주인, 트리거, 모드 선언은 [ADR-0017](0017-document-workflow-triggers-and-executor.md)에서 다룬다.

### 추천 — [agent]

- **허용 최대 모드를 둔다(Q7).** 문서나 workspace 의 owner 가 그 문서에서 쓸 수 있는 가장 강한 모드를 정한다.
  - 예: 공유 문서는 "편집 수락"까지, 개인 문서와 local 범위 문서는 제한 없음.
  - 권한(상한)은 "무엇을 할 수 있나", 허용 최대 모드는 "얼마나 묻지 않고 할 수 있나"를 정한다. 둘 다 owner 쪽이 정하고, 요청자는 그 안에서 고른다.
- **모드는 문서별 기본값으로 기억한다(Q8).** 요청할 때는 기본값보다 한 단계 낮추는 것만 허용한다. 올리려면 문서 기본값을 바꾼다.
- **위험도는 서버 규칙으로 판정한다(Q13).** 에이전트가 스스로 판단하게 하면 제품이 강제할 수 없다.
- 근거
  - 공유 문서에서 사람이 모르는 사이에 글이 바뀌는 일은 되돌리기로 완전히 복구되지 않는다. 신뢰가 먼저 깨진다. 허용 최대 모드가 이 위험을 문서 단위로 막는다.
  - 모드는 확인 강도만 바꾸고 권한을 넓히지 않는다. 그래서 ADR-0012 의 policy 한 곳에서 판정하는 구조가 유지된다.

## 후보안 4: 표시와 되돌리기

### 참여 표시 (presence)

- 에이전트도 presence 에 참여자로 보인다. 사람과 같은 자리에 두되, **에이전트임을 표시**한다.
  - delegated agent 는 "누구의 에이전트인지"를 함께 보인다. 예: "리뷰 에이전트 (홍길동 대리)".
  - 신원은 서버가 인증된 principal 로 정한다. 에이전트가 주장한 이름을 쓰지 않는다(ADR-0012 §1, G1 추인 대기).
- P2/P3 로 붙은 에이전트는 WebSocket 연결이 없다. **서버가 에이전트 대신 presence 를 발행**한다.
  - 에이전트가 문서를 읽거나 편집 중일 때 참여자 목록에 나타나고, 편집하는 범위에 커서나 강조를 둔다.
  - 연산이 끝나고 일정 시간이 지나면 사라진다.
- 모양의 세부(배지, 색, 커서 모양)는 UI 원칙(ADR-0014)과 `DESIGN.md` 안에서 G1 으로 정한다.

### 변경 표시

- **제안**: 본문 안에 변경안을 표시하고 수락과 거절을 제공한다. 누가 만든 제안인지 보인다.
- **직접 편집**: 에이전트가 최근에 바꾼 범위를 잠깐 강조한다. 문서의 이력 목록에서 "에이전트 변경"을 따로 볼 수 있다.

### 되돌리기

- 사람의 실행 취소(Ctrl/⌘+Z)는 **자기 편집만** 되돌린다. 에이전트 편집을 되돌리지 않는다. 지금 Yjs 의 기본 동작과 같다.
- 에이전트 편집은 **변경 묶음(한 번의 요청으로 생긴 편집 전체) 단위로 되돌린다.**
  - 되돌리면 그 묶음이 넣은 텍스트를 지우고 지운 텍스트를 되살린다.
  - 그 뒤에 사람이 같은 범위를 고쳤으면 자동으로 되돌리지 않는다. 겹치는 곳을 보여주고 사람에게 확인한다.
- 되돌릴 수 있는 사람: 그 문서에 `content.write` 가 있는 사람. delegated agent 의 변경은 위임한 사용자도 되돌릴 수 있다.

## 후보안 5: 이력과 출처

Yjs update 에는 작성자가 없다. 그래서 "누가 썼는가"를 어디에 기록할지 정해야 한다.

### H1. 연결 principal 로 update 에 작성자를 붙인다

- 서버가 update 를 받을 때, 그 연결의 principal 을 audit log 에 함께 기록한다.
- 장점: 모든 쓰기 경로(사람, 에이전트)에 똑같이 적용된다. 따로 모델을 만들지 않는다.
- 단점: 기록 단위가 CRDT update 라서 사람이 읽을 수 있는 "변경"이 아니다. 되돌리기와 제안 단위와 맞지 않는다.

### H2. 에이전트 변경 묶음을 제품 개념으로 둔다 (추천)

- 에이전트 요청 한 번이 하나의 **변경 묶음**이 된다. 묶음에는 다음을 남긴다.
  - actor `{ principal, onBehalfOf? }`
  - 대상 문서, 시각
  - 반영 방식(제안인지 직접인지)과 상태(대기, 수락, 거절, 적용, 되돌림)
  - 사람이 읽을 수 있는 변경 내용(텍스트 diff)
  - 에이전트가 남긴 설명(무엇을 왜 바꿨는지)
- 장점: 제안, 직접 편집, 되돌리기, 이력 표시가 **같은 단위**를 쓴다. audit log(ADR-0012 §5)의 에이전트 쓰기 기록도 이것을 가리키면 된다.
- 단점: 새 개념과 저장소가 필요하다. domain 문서와 모델 변경이 따른다.

### checkpoint 와의 관계

- checkpoint 는 사람이 의도적으로 만드는 이력이다. 자동 저장을 checkpoint 로 올리지 않는다는 기존 제품 원칙이 있다.
- 추천: 변경 묶음은 checkpoint 와 **다른 이력 항목**이다. 문서 이력 화면에서 checkpoint 와 에이전트 변경을 함께 보여주되 섞지 않는다.
- 큰 직접 편집 전에 자동으로 checkpoint 를 만들지 않는다(Q9, 결정됨). checkpoint 는 사람이 의도적으로 만드는 이력으로 남는다.

### 사람 편집의 작성자

- 이 ADR 은 에이전트 편집만 변경 묶음으로 추적한다. 사람 편집의 문자 단위 작성자 기록(blame)은 범위 밖이다.

## 결정 요약 — [user] 2026-09-26 (추천안 채택)

| 축 | 결정 |
| --- | --- |
| 연결 | 제품이 정의한 문서 연산 API 가 공개 계약이다. 첫 번째 연결 방식은 MCP 다. Yjs 직접 참여는 공개 계약으로 두지 않는다. |
| 인증과 권한 | delegated 는 동의 흐름으로 받은 scope token, member 는 admin 이 등록한 서비스 자격증명. `content.suggest` action 을 새로 둔다. |
| 편집 반영 | 모드 단계(수동 / 편집 수락 / 자동 / 자동+크루즈 / 모두 허용)로 둔다. 실제 동작은 min(권한, 모드)다. owner 가 허용 최대 모드를 정하고, 모드는 문서별 기본값으로 기억하며, 위험도는 서버 규칙으로 판정한다. E3 는 별도 모드로 두지 않는다. |
| 표시와 되돌리기 | 서버가 에이전트 presence 를 발행하고 에이전트임을 표시한다. 사람의 실행 취소는 자기 편집만, 에이전트 편집은 변경 묶음 단위로 되돌린다. |
| 이력과 출처 | 에이전트 변경 묶음을 제품 개념으로 두고 제안, 직접 편집, 되돌리기, audit 가 같은 단위를 쓴다. checkpoint 와 구분한다. |

## 결정된 질문 — [user] 2026-09-26

모든 질문을 추천안대로 확정했다. **(선택)** 표시는 추천이 명시되지 않아 초안에 가장 가까운 안을 택한 항목이다.

1. **공개 연결 계약**: 문서 연산 API(application use case)를 공개 계약으로 두고, 첫 연결 방식은 MCP 다. Yjs 직접 참여는 공개하지 않는다.
2. **Yjs 직접 참여의 여지**: 외부 에이전트에는 열지 않는다. 제품이 운영하는 신뢰된 에이전트에 한해 나중에 다시 볼 여지만 남긴다. 지금은 모든 에이전트가 같은 API 로 붙는다.
3. **local 범위 문서와 로컬 에이전트 (선택)**: (a) opt-in sync 를 켠 local 문서에만 서버를 통해 붙는다. 초안에서 "처음 구현이 가장 단순하다"고 적은 안이고, ADR-0011 의 "sync 한 local 문서는 서버가 읽을 수 있다"는 결정과 맞는다. 기기 안의 로컬 연결 지점(b)은 데스크톱 패키징 뒤에 다시 본다.
4. **delegated agent 연결 방식**: 동의 흐름으로 받는 scope token 만 둔다. 개인 access token 은 두지 않는다.
5. **제안 권한**: `content.suggest` 를 새 action 으로 둔다. 처음에는 editor 이상만 제안할 수 있다. viewer 의 제안이나 제안 전용 역할은 필요할 때 다시 본다.
6. **편집 기본 동작**: 하나로 고정하지 않고, 권한 모델에 따라 달라지는 모드 단계(수동 / 편집 수락 / 자동 / 자동+크루즈 / 모두 허용)로 둔다. 권한(상한)과 모드(선택)를 두 축으로 나누고 실제 동작은 min(권한, 모드)다(후보안 3).
7. **허용 최대 모드**: 둔다. 문서나 workspace owner 가 그 문서의 허용 최대 모드를 정한다. 예: 공유 문서는 "편집 수락"까지, local 범위와 개인 문서는 제한 없음.
8. **모드를 기억하는 단위**: 문서별 기본값을 둔다. 요청할 때는 기본값보다 한 단계 낮추는 것만 허용한다.
9. **이력**: 에이전트 변경 묶음은 checkpoint 와 별도 이력 항목이다. **(선택)** 큰 직접 편집 전에 자동 checkpoint 를 만들지 않는다. 자동 저장을 checkpoint 로 올리지 않는다는 기존 원칙을 따른다.
10. **에이전트 설명의 보존 (선택)**: 변경 묶음에 에이전트가 남기는 설명(무엇을 왜 바꿨는지)은 저장한다. 초안의 변경 묶음 항목에 있던 내용이다. 사용자의 원래 요청 문장은 기본으로 저장하지 않는다.
11. **도입 순서**: 수동과 편집 수락을 먼저 지원한다. 그다음 자동, 마지막으로 자동+크루즈와 모두 허용을 붙인다.
12. **크루즈의 종료, 중단, 양보 (선택)**
    - 크루즈를 시작할 때 종료 조건 세 가지(시간, 변경량, 범위)를 모두 정한다. 요청자가 정하지 않으면 제품 기본값을 쓴다. 기본값 숫자는 구현할 때 G1 으로 정한다.
    - 요청자와 그 문서에 `content.write` 가 있는 사람은 언제든 크루즈를 멈출 수 있다. 멈춤은 presence 와 변경 표시에서 바로 할 수 있어야 한다.
    - 사람이 같은 구간을 편집하기 시작하면 그 구간은 건너뛰고, 할 일을 제안(변경 묶음)으로 남긴 뒤 다음 작업으로 넘어간다. 초안의 "확인이 필요한 항목은 제안으로 쌓고 넘어간다"와 같은 규칙이다.
13. **위험도 판정 위치**: 서버 규칙으로 판정한다. 제품이 강제할 수 있어야 한다.

## 결과

### 이 결정과 독립적으로 진행할 수 있는 작업

- 슬라이스 2(동시편집): Hocuspocus provider, 협업 연결 인증, awareness 로 원격 커서 표시.
- ADR-0012 §1 구현: 단일 `authorize`, 서명된 collab token, `onAuthenticate`, internal 서비스 인증.
- presence 에 principal 유형(사람, 에이전트)을 담을 자리를 두는 것. 표시 모양은 ADR-0014 와 `DESIGN.md` 안에서 G1 으로 정한다.

### 이 결정에 따라 할 작업

- 에이전트 principal 과 자격증명 구현(ADR-0012 결과 4 번과 함께).
- 문서 연산 use case 와 MCP adapter.
- 제안 모델, 변경 묶음 모델, 되돌리기.
- 모드 단계, 허용 최대 모드, 서버 위험도 규칙, 크루즈의 종료와 중단.
- `docs/domain/` 갱신: 변경 묶음과 제안 모델, `content.suggest` action, 권한과 모드의 두 축, principal 문서의 편집 모드 규칙.
  domain model 변경이므로 plan 과 함께 진행한다.

### 트레이드오프와 설계 부담

- **제안의 위치는 문서 타입이 안다.** core 는 타입을 모른다(ADR-0013). 텍스트 범위에 붙는 제안과 diff 는 `markdown`/`code` type module 이 제공해야 한다. core 는 변경 묶음의 저장, 권한, 상태만 맡는다.
- 제안과 변경 묶음을 Yjs 문서 안에 둘지(예: 별도 루트) 서버 저장소에 둘지는 구현 설계에서 정한다. local 범위에서도 동작해야 한다는 점이 제약이다.
- 수동 모드와 크루즈는 제안 모델이 먼저 필요하다. Q11 의 순서대로 수동부터 지원하므로, 제안 모델이 첫 출시의 선행 작업이다.
- 모드가 다섯 단계라 요청자와 협업자가 지금 어떤 모드인지 알 수 있어야 한다. presence 나 변경 표시에 모드를 드러내는 UI 가 필요하다.
- MCP 명세가 바뀌면 adapter 를 고쳐야 한다. 계약(use case)은 영향을 받지 않는다.

## 검증 방법 (구현 뒤)

- delegated agent 가 위임한 사용자의 권한을 넘는 문서를 읽거나 쓰려 하면 거부된다. 사용자 권한을 줄이면 에이전트의 다음 요청부터 거부된다.
- 제안만 허용된 에이전트가 "모두 허용" 모드로 요청해도 제안으로 처리된다(min(권한, 모드)).
- 허용 최대 모드가 "편집 수락"인 문서에서는 그보다 강한 모드를 고를 수 없다.
- 자동+크루즈 모드에서 확인이 필요한 변경은 제안으로 쌓이고 작업은 계속된다. 종료 조건에 닿거나 사람이 멈추면 즉시 멈춘다.
- 두 사람이 같은 문서를 보는 동안 에이전트가 제안을 만들면, 두 사람 모두 본문은 그대로이고 제안이 보인다. 수락하면 두 사람 모두에게 반영된다.
- 에이전트의 직접 편집 뒤 사람이 Ctrl/⌘+Z 를 누르면 자기 편집만 되돌아간다. 변경 묶음 되돌리기는 에이전트 편집만 되돌린다.
- 이력 화면에서 에이전트 변경마다 에이전트, 위임한 사용자, 시각, 변경 내용이 보인다.

## 참고 자료

아래 과거 backlog 는 요구사항이 아니라 참고 자료다.

- [REQ-DEFERRED-COMMENTS-SUGGESTIONS](../archive/requirements/backlog/REQ-DEFERRED-COMMENTS-SUGGESTIONS.md): 제안과 댓글을 보류했던 기록. stale anchor 정책을 따로 설계해야 한다고 적었다.
- [REQ-DEFERRED-EXTERNAL-WORKFLOW-INTEGRATIONS](../archive/requirements/backlog/REQ-DEFERRED-EXTERNAL-WORKFLOW-INTEGRATIONS.md)
- [REQ-DEFERRED-WORKFLOW-HOOKS](../archive/requirements/backlog/REQ-DEFERRED-WORKFLOW-HOOKS.md)

## 관련 문서

- 인터뷰/의도 기록: [2026-09-24 제품 방향 인터뷰](../direction/2026-09-24-product-direction-interview.md) §1, §4
- 도메인: [principal](../domain/models/principal.md), [collaboration boundaries](../domain/rules/collaboration-boundaries.md)
- 관련 ADR: [ADR-0010](0010-decision-gates-and-provenance.md), [ADR-0011](0011-data-authority-by-scope.md), [ADR-0012](0012-authorization-policy-and-principals.md), [ADR-0013](0013-document-type-model.md), [ADR-0014](0014-ui-principles.md)

## 변경 이력

| 날짜 | 변경 | 결정자 |
| --- | --- | --- |
| 2026-09-26 | 최초 제안 (proposed). 추천은 문서 연산 API + MCP, 기본 제안, 권한으로 직접 편집 허용, 변경 묶음 | agent:claude-code |
| 2026-09-26 | 편집 기본 동작을 모드 단계(수동 / 편집 수락 / 자동 / 자동+크루즈 / 모두 허용)로 둔다. "권한으로만 정한다"는 추천을 대체하고, 권한(상한)과 모드(선택) 두 축으로 다시 썼다. Q6 결정, Q7·Q8·Q11–Q13 갱신 | user (모드 단계) / agent:claude-code (두 축 구조, 모드별 동작 초안) |
| 2026-09-26 | 워크플로우가 에이전트를 실행하는 시작 방식과 ADR-0017 링크를 추가 | agent:claude-code |
| 2026-09-26 | 남은 질문 Q1–Q5, Q7–Q13 을 모두 추천안대로 확정하고 accepted 로 바꿨다. 추천이 명시되지 않은 Q3, Q9 일부, Q10, Q12 는 초안에 가장 가까운 안을 택했다 | user |
