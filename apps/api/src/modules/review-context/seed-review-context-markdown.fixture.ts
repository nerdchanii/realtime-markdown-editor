export const reviewPlanMarkdown = `# Review Plan

## CE evidence path

- CE-01: open the same workspace document in two sessions and confirm convergence.
- CE-02: verify remote cursor and selection labels use workspace member identity.
- CE-03: edit while disconnected, reconnect, and confirm local plus remote text remains.
- CE-04: open checkpoint history and inspect the saved snapshot.
- CE-05: switch to split preview and confirm standard Markdown renders.

## Product context

This seeded document keeps project properties outside the Markdown body and links to [Decision Log](./decision-log.md).
`;

export const decisionLogMarkdown = `# Decision Log

The launch readiness review starts from [Review Plan](./review-plan.md) so backlinks can show incoming context without requiring wikilinks.
`;
